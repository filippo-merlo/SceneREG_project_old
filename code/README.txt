object_scenes_cooccurrency.pkl: pandas datadframe with the cooccurrency of objects in scenes rows = object index, columns = scene name
tf_idf_scores.pkl: pd dataframe with the tf-idf scores of objects in scenes rows = object index, columns = scene name

object_scene_names.pkl: dict with: list of object names [the idx rows of the dataframes correspond to the name of the object in this list], list of scene names